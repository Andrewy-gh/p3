{
  "name": "backend",
  "type": "module",
  "scripts": {
    "dev": "bun run --hot --env-file=.env.local src/index.ts",
    "build": "tsc",
    "start": "bun run --hot --env-file=.env.local src/index.ts",
    "script": "bun run --hot --env-file=.env.local src/evals/1.ts",
    "eval:dev": "bun run --env-file=.env.local evalite watch",
    "main": "bun run --hot src/main.ts",
    "test": "bun test"
  },
  "dependencies": {
    "@ai-sdk/google": "^2.0.12",
    "@hono/zod-validator": "^0.7.2",
    "ai": "^5.0.35",
    "autoevals": "^0.0.131",
    "evalite": "^0.11.5",
    "hono": "^4.9.6",
    "hono-rate-limiter": "^0.4.2",
    "vitest": "^3.2.4",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@types/bun": "^1.2.21",
    "jsdom": "^26.1.0",
    "tsx": "^4.7.1",
    "typescript": "^5.8.3"
  }
}
